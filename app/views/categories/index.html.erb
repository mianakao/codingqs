<h1>Listing categories</h1>

<table class="table table-condensed">
  <tr>
    <th>Name</th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
  </tr>
<% end %>
</table>